{
  "name": "warmup-worker",
  "version": "1.0.0",
  "description": "A simple mental math game as a Cloudflare Worker",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:install": "playwright install"
  },
  "devDependencies": {
    "wrangler": "^4.38.0",
    "@playwright/test": "^1.40.0"
  }
}